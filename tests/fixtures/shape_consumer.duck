const { struct } = import "duck:prelude" ()

const provider = import "./shape_provider.duck"
const { make } = provider()

type Shape = struct { .size = Int }

extend Shape {
  type Out = Int
  .measure = (s: Shape) => 2
}

let local: Shape = [.size = 1]
make() + Measure.measure(local)
