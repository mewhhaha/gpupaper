# Blot syntax fixture

`grammar.baba` and `baba.json` are copied from `../blot` commit
`47fa57773fa4a00bc078fc92bf55ab8bef3e75d3`. The generated pair is local so the
GPU-syntax conformance gate is reproducible without depending on a sibling
checkout.

Regenerate it with `deno task blot:grammar`.
