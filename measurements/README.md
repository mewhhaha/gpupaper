# Measurements

This directory contains machine-readable records produced by
`benchmark:record`. Historical frontend-specific records live with the
frontends that own those workloads.

Records committed here must satisfy `PERFORMANCE.md`, include raw observations,
and distinguish admissible measurements from diagnostics collected under
contention.
