module (!init: Init) where

declare effect Input {
  step: () => I32
}

declare Init { input: Input }

let total = 0
step <- Input.step()

for value in 0..10 by step {
  total = total + value
}

let result = total
return { .result = result }
