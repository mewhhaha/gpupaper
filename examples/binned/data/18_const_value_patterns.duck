const alternate = 'w'

// The sigil distinguishes a captured constant from a new pattern binding.
let classify = (key: Char) => match key {
  | 'q' | #(alternate) => 40
  | _ => 0
}

let computed = match 3 {
  | #(1 + 2) => 2
  | _ => 0
}

classify 'w' + computed
