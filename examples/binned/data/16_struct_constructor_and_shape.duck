const { struct } = import "duck:prelude" ()

type Point = struct { .x = I32, .y = I32 }

const make_point = Point.new
const x_type = Point.shape.x
let x: x_type = 40
let point = make_point { .y = 2, .x = x }

point.x + point.y
