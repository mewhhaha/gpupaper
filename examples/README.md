# Examples

[`zero/`](zero/) is the canonical consumer example. It contains the complete
Zero frontend, its Baba grammar input, a benchmark program, and an equivalent
Rust workload.

The other directories are retained compatibility and conformance corpora. They
are test inputs rather than examples of the public gpupaper integration path.
